# ── core ─────────────────────────────────────────────
streamlit==1.28.0
plotly==5.17.0
pandas==2.3.1
setuptools==69.5.1 

# ── data download ────────────────────────────────────
pandas-datareader==0.10.0      # Stooq feed
yfinance==0.2.18               # only for company meta, safe to keep
requests==2.32.4

# ── nice-to-have (already in your file, leave them) ─
nsetools==1.0.12